# Copy this file to .local/context-panel-runtime.env for local debug installs.
# The real .local/context-panel-runtime.env file is gitignored.

# Google Antigravity quota refresh now uses Antigravity's local Keychain login.
# There are no Context Panel Google OAuth build settings for local installs.
