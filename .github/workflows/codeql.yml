---
name: CodeQL

"on":
  pull_request:
  push:
    branches:
      - main
  schedule:
    - cron: "31 9 * * 1"
  workflow_dispatch:

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  analyze:
    name: Analyze Swift
    runs-on: macos-latest
    timeout-minutes: 45

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: swift

      - name: Build
        run: swift build --target ContextPanelCore

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v4
